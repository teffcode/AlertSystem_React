import React from 'react'
import { Search, Input, GridColumn } from 'semantic-ui-react';

const SearchExampleInput = () => (
       <Input  style={{height:35}}  action={{ icon: 'search' }} />
  
);


export default SearchExampleInput;